language: node_js
node_js:
  - v12
  - v10
cache:
  directories:
    - "node_modules"

before_install:
  - cp .env.example .env

install:
  - npm install

script:
  - npm run lint
  - npm run test
  - npm run test:coverage
